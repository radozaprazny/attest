# Claude Code — local settings (permission allowlist)
.claude/settings.local.json

# Ignored scratch — /gate's fallback material and the ship guard's decision log. The run
# records beside it ARE committed (attest ADR-0016, ADR-0026, ADR-0034)
.attest/tmp/
