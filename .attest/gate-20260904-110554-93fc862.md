# gate run — 2026-09-04T11:05:54Z

- HEAD: 93fc862 (fix/guard-trace-and-semantics) · tree: dirty — gated the working diff (phase 12)
- kit: 0.5.0
- passes: reviewer ran · business degraded (no filled BUSINESS.md — "nothing declared") · decision ran · compliance ran
- verdict: ⚠️ commit after changes
- findings: 1 blocker · 5 major · 14 minor · 4 nit
  - blocker · /decision (code half: reviewer) · `template-cleanup.sh` deleted a record on **any**
    non-zero `git cat-file` — 128 outside a repository, 127 with no git — so wherever git could
    not answer it removed every record including the adopter's own, the exact inversion of the
    invariant ADR-0040 had just written down. Now: prove git can answer, then act only on a
    specific negative
  - major · reviewer · the smoke fixture for that sweep was not a git repository, so all three
    new assertions passed **through** the bug and the sha discriminator was never exercised —
    a green suite over a destructive defect. The fixture now has its own commit, and asserts
    both halves plus the outside-a-repo case
  - major · /decision · the append-only carve-outs lived only in the ADR log; ADR-0016, 0026 and
    0034 had each propagated theirs into the ladder and `/gate`. Both now name all three
  - major · /business · README and GUIDE advertised coverage the literal matcher does not have,
    and this change made the log look complete while a non-matching command still writes nothing
  - major · /compliance · the redaction reaches the working tree, not the blob at the preceding
    commit, while three places stated the matter closed. Recorded as an accepted risk of the
    class already accepted for the 45 authorships, before a visibility flip rather than after
  - major · /compliance · the trace keeps `@ . - _ : =` — an address or a credentialed URL
    survives verbatim in a persistent, unbounded local file, and this change widened what is
    traced. GUIDE 2.2 says so now; the shipped `COMPLIANCE.md` names the file in §7
  - minor ×14 · pre-ADR-0037 semantics in four places incl. the hook's own header; the prompt
    still equating "a record exists" with safe; `install.sh` recording the file as landed after
    the rewrite that could truncate it; `.gitattributes` never reaching the adopter's repo;
    the CRLF fixture built with a GNU-only `sed`; the email assertion keyed on a domain
    allowlist (and adding the maintainer's domain to a shipped file to do it); stale version and
    assertion counts in the carrier; 0.5.0 unexplained; README's cleanup description
  - nit ×4 · two overlong lines, a dead `cp` in the CRLF fixture, scope mismatch between the
    strip (`*.sh`) and the attribute (`.claude/hooks/*`)
- note: every finding was addressed before commit. The blocker and the first major were both
  introduced by this series and caught by the kit's own gate — the sweep now refuses to act
  outside a git checkout, and its test runs inside one. Baseline re-run green afterwards:
  shellcheck clean, smoke 118 → **149** passed / 0 failed. `ensure_attribute` gained an
  awk exact-match after a glob-as-regex bug duplicated its lines on every re-run.
