# ship audit — 2026-09-24 (/audit-history, default mode)

- HEAD: 67c8edc (fix/guard-powershell-and-non-git) · tree: clean · nothing untracked
- findings: 0 blocker · 0 major · 0 minor
- kit: 0.12.0
- mode: default — scope `HEAD --not --remotes` (two commits, f5134a9..67c8edc, base 79cb7f1 on
  origin/main), the working tree, the index and untracked files (none). No push target exists
  yet, so `@{push}` was not available. Run by the session that wrote the change, not by an
  independent pass.
- verdict: ✅ clean — nothing found that must not ship
- key layer: betterleaks 1.8.1 — unpushed commits exit 0 · unstaged and staged: tree clean,
  nothing to scan · untracked: none, so the `dir` call was skipped as step 4 says.
- scanned, empty: special-category data · national identifiers · personal names, emails, phone
  numbers and IP addresses in the 242 added lines · client and third-party names, the adopter
  included · internal hostnames and absolute machine paths, Windows-style ones included · URLs ·
  large or binary files. Pattern hits triaged as not data: three dates and a record id shaped
  like phone numbers.
- declarations read: `BUSINESS.md` and `COMPLIANCE.md` are the shipped templates; attest's own
  posture (`docs/attest-progress.md`, out of scope) is unchanged by this branch.
- authorship: both commits carry the maintainer's own name and address — intended, in their own
  public repository.
- degraded: none.
- remediation: none required.
- note: written before the push it gates (ADR-0033), so it lands under a later sha than it names.
