# ship audit — 2026-09-12 (/audit-history, default mode)

- HEAD: 7a5cb60 (integration) · tree: clean · nothing untracked but two ignored files
- kit: 0.8.0
- findings: 0 blocker · 1 major · 2 minor
- mode: default — scope `origin/main..integration`: 20 commits, 21 files, the union of PR #19,
  PR #18 renumbered, and this branch's own six. Commit messages, file names and content, plus
  the working tree and index. Run by an independent pass, not by the session that wrote the
  change (ADR-0051).
- verdict: ⚠️ one major, found and closed **before** the push — nothing in the pushed state
  matches it
- the major: a third party's project name and three of its specific weaknesses had been removed
  from the tree and still stood in two **unpushed** commits, which a push would have made
  permanent and world-readable. Class: client/customer confidentiality plus the dated inventory
  of another codebase's faults that ADR-0049 refuses. Closed by rebuilding the two commits
  (`5bbad84` → `4979896`); `.attest/gate-20260912-194120-4979896.md` declares that rewrite in
  its own first lines. Re-verified after the fix by grepping the whole branch's patches **and**
  commit messages: 0 occurrences. That re-verification is a grep, not a second full pass — said
  plainly, because the difference matters.
- the minors, carried to `docs/attest-progress.md` *Next*, not fixed: a `Claude-Session:` trailer
  on seven commits arriving from the PR branches, already public and the maintainer's own
  (the class ADR-0040 weighed); and the adopter's timestamps in the devlog, kept deliberately —
  they are what the argument rests on, and without the name they identify nobody.
- degraded: one pass — `BUSINESS.md` and `COMPLIANCE.md` are unfilled kit templates by design
  (ADR-0006), so there are no project-specific data classes to scan against. Everything else ran
  in full.
- scanned, empty on the added lines: secrets and credential assignments · special-category data ·
  national identifiers · personal names and addresses · client names · IP addresses · email
  addresses · internal hostnames and absolute machine paths · stray large, binary or data files.
- remediation: none outstanding.
- note: written before the push it gates (ADR-0033), so it lands under a later sha than it names.
