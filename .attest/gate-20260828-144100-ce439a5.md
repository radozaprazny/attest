# gate run — 2026-08-28 (second pass; recorded retrospectively)

> **Honesty note.** This record was written at 2026-08-28T14:41Z, after the fact — the run
> itself happened between the 12:57Z and 14:38Z records and its exact start was not captured,
> so the timestamp in this filename is when the record was *written*, not when the gate ran.
> The record is here because a run without one breaks the chain `.attest/` exists to keep;
> a late record that says it is late is worth more than a gap or a plausible-looking time.

- HEAD: ce439a5 (feat/lean-kit) · tree: dirty — gated the working diff (phase 11, the lean-kit series)
- kit: 0.3.0
- passes: reviewer ran · business degraded (no filled BUSINESS.md — "nothing declared") · decision ran · compliance ran
- verdict: ⚠️ commit after changes
- findings: 0 blocker · 2 major · 10 minor · 1 nit
  - major · /compliance audit · `business/SKILL.md` Mode 3 still deferred regulated ground to
    `/compliance` unconditionally, and the exception that fixed it sat *outside* Mode 3 — the
    section `/gate` hands to the subagent. A rule stated anywhere else in the file never reaches
    the pass that must apply it
  - major · /compliance audit · the absent-owner fallback enumerated `/decision` (a choice) and
    `/audit-history` (bytes), which between them do not cover a bare new personal-data field —
    the fallback was not total, so a hunk could still fall through
  - minor ×10 · reviewer: the documented 40-line cap was the sum of three content caps (real bound
    ~52), the `--dry-run` bullet still described the pre-fix behaviour, GUIDE §3.2 still pointed at
    the deleted Stop hook, a kept `settings.json` was listed under two opposite framings ·
    /decision: ADR-0028 did not record the two behavioural rules the fixes added, ADR-0030 narrowed
    ADR-0023 with no cross-reference either way, the `/business` pointer appeared in no ADR and not
    in the ladder, and ADR-0027/0029/0031 carried no `Supersedes:` line · /business: README's
    "First 5 minutes" told template-path readers to run an installer that path deletes
  - nit ×1 · an unreachable `drift)` arm in the installer's settings branch
- note: reviewer returned ✅ on this pass; both majors were `/compliance`'s and both were holes
  opened by the fixes made after the 12:57Z run. All findings were addressed, and one of those
  fixes introduced a `set -e` regression in `install.sh` that `smoke.sh` caught immediately.
  Superseded in practice by the 14:38Z run, which gated the corrected tree.
