# ship audit — 2026-08-31 (/audit-history, default mode)

- HEAD: 8a7d45a (feat/lean-kit) · tree: clean · nothing untracked
- kit: 0.3.0
- mode: default — scoped **wider** than the skill's minimum: `main..HEAD` (2 commits, 32 files,
  +1453/-802), i.e. the whole delta the PR proposes, not only `@{push}..HEAD` (which is just
  8a7d45a). The branch was already pushed at 9101eac, so the narrow scope would have audited
  one doc commit while the phase-11 change itself went unexamined by this gate.
- verdict: ✅ clean — nothing found that must not ship
- findings: 0 blocker · 0 major · 0 minor · 0 nit
- degraded: **step 2 (read the project's own boundaries) had nothing to read.** `BUSINESS.md`
  Non-goals and `COMPLIANCE.md` are still unfilled kit templates in attest's own checkout — by
  design (ADR-0006: the root docs are the product, not attest's status), so no project-specific
  leak class could be added on top of the generic taxonomy. attest's real posture is the first
  Notes bullet in `docs/attest-progress.md`, and it declares no data class at all. This is the
  one pass that degraded; everything else ran in full.
- what was scanned, and what the hits were:
  - **secrets / keys** — the only matches for `secret|token|password|BEGIN … PRIVATE KEY|AKIA|ghp_`
    are prose *about* secrets (`ship_guard.sh` header, GUIDE PART 4, the `.mcp.json` warning) and
    one smoke fixture literal, `BUSINESS.md` containing the words "secret sauce" — used to assert
    the declaration hook does *not* print a section it was not asked for. No `.env`, `.pem`,
    `.key`, `id_rsa` or credential file is tracked.
  - **special-category data · national identifiers** — no match for birth-number, passport or
    tax-number shapes.
  - **ordinary personal data** — one email in the tree: `smoke@example.invalid` in
    `scripts/smoke.sh:106`, a reserved-TLD test address set as a fixture repo's git identity.
    Intended. The maintainer's own name and address appear in commit authorship of a repo he is
    publishing — noted, not a finding, per the taxonomy's context rule.
  - **client / customer names** — none; the repo names no third party.
  - **IP addresses · geolocation · device identifiers** — no match.
  - **internal hostnames / absolute paths / usernames** — no match for `/home/<user>`,
    `/Users/<user>` or `C:\Users`. The one file holding an absolute local path,
    `.claude/settings.local.json`, is gitignored and untracked.
  - **stray large / data files** — largest tracked file is `docs/attest-decisions.md` at 64 KB;
    everything tracked is markdown or shell.
- remediation: none required. Nothing to rotate, nothing to purge from history.
- note: written **before** the push and the PR it gates, so the sha in this filename is the
  state actually leaving the machine. It is therefore untracked at the moment the ship guard
  reads it — the guard checks the filesystem, not the index, and a record cannot name the commit
  that contains it. Committing it necessarily lands it under a later sha.
