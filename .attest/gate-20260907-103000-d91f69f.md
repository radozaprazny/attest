# gate run — 2026-09-07T10:30:00Z

- HEAD: d91f69f (fix/guard-trace-and-semantics) · tree: dirty — gated the working diff
- kit: 0.5.0
- passes: reviewer ran · business degraded (no filled BUSINESS.md — "nothing declared"; audited against README §"Not a kitchen sink", GUIDE PART 2 and the carrier's standing constraints) · decision ran · compliance ran
- verdict: ⚠️ commit after changes
- findings: 2 blocker · 2 major · 7 minor · 1 nit
  - blocker · reviewer · `scripts/template-cleanup.sh:125` + `.github/workflows/template-cleanup.yml:46`
    — `actions/checkout@v4` defaults to `fetch-depth: 1`, so `git rev-parse --git-dir` succeeds
    while `git cat-file -e <sha>^{commit}` fails for every commit but HEAD. Records name an
    ancestor by construction (ADR-0033), so the sweep deletes **the adopter's own** records and
    `rmdir`s `.attest/`. Reproduced here: shallow clone → guard passes, resolve fails. Falsifies
    `README.md:139`, `audit-ladder.md:157`, `gate/SKILL.md:142` ("a record you wrote is kept").
    Runs unattended with a write token in someone else's repository.
  - blocker · /business audit · `.gitattributes:10` ships a blanket `*.sh text eol=lf`; the file
    is tracked, so the template button copies it, and `template-cleanup.sh` never narrows it
    (0 mentions). Every `.sh` an adopter writes is then normalised by a rule the kit put there —
    the non-goal at `README.md:88` / `GUIDE.md:95`. The installer path was already narrowed to
    `.claude/hooks/*` for this exact reason (`install.sh:343`, ADR-0039); the template path was
    not. A violated non-goal is always a blocker.
  - major · reviewer · `install.sh:118-133` — the CRLF strip runs only in `copy_if_absent`'s
    fresh-copy branch. An upgrade over an existing CRLF hook keeps all 166 CRs, is reported as
    ordinary `drift` with "diff against …" (a whitespace-only difference most tools hide), and
    leaves `dash` exiting 2 — the blocking failure `.gitattributes:6-8` describes. That is
    precisely the population ADR-0039 exists for.
  - major · /decision audit · the log gained a third relation, `Narrows:` (header rule at
    `docs/attest-decisions.md:12-16`, used at `:1057`, `:1092`), but that choice has no entry —
    no alternatives weighed — and the rule never reached the shipped kit: `DECISIONS.md:20,37`
    and `decision/SKILL.md:67-71` still know only `Supersedes` and the `Status` flip. attest's
    practice and the kit's rules diverged silently.
  - minor ×7 · /compliance: the ladder states the ADR-0040 exception more broadly than both the
    ADR and `gate/SKILL.md:139-141` — it drops "the finding, its counts and its verdict are never
    touched" and "one record, not a licence to tidy `.attest/`", and the ladder is the copy that
    installs and is read at runtime · ADR-0040 ships no mechanism: an unmarked redaction is
    undetectable from `.attest/` · `smoke.sh:513`'s address grep runs after `template-cleanup`
    swept the records, so it is blind to the only file that carried the address ·
    reviewer: docs say 156 assertions, measured 157 (`attest-progress.md:47,:92`,
    `attest-devlog.md:158`) · `.attest/*.md` is not pinned to LF, so on a CRLF checkout the
    `- HEAD: $SHA` arm is dead and the guard's stated reason misleads (fail-closed, not a bug) ·
    the `d91f69f` ship record's scope line is now stale (`+867/-67`, `tree: clean`) ·
    /decision: ADR-0038 records the `tr` keep-set as `@ . - _ : =` while the code also keeps
    space and `/`, i.e. whole paths (`ship_guard.sh:65`)
  - nit ×1 · reviewer · lines of 208–248 chars introduced by the series in `ship_guard.sh:153,164`
    and `audit-ladder.md:65,66`, against ~95–100 in the surrounding prose
- note: **scope was widened deliberately.** The skill's step-1 command gates the working diff,
  which here was one edit; this run gated `origin/main..working tree` — 19 files, +879/-67, the
  whole of PR #10 — because two earlier runs (`gate-20260904-110554`, `gate-20260906-113757`)
  covered phase 12 as a *dirty tree* and nothing had gated the committed result. Also worth
  recording: `gate-records.txt` built by the skill's own `ls | tail -3` is alphabetical, so ship
  records crowd out every gate record; the passes were handed a corrected list, and the defect
  is already on the carrier's P1 queue.
- note: two of the four passes' findings were reproduced independently before being written here
  — the shallow-clone deletion (fixture: two commits, a record naming the first, `--depth 1`
  clone) and the `.gitattributes` reach (tracked file, 0 mentions in the cleanup).
