# gate run — 2026-09-07T11:08:42Z (second pass, over the fixes for the 10:30Z run)

- HEAD: d91f69f (fix/guard-trace-and-semantics) · tree: dirty — gated the working diff (the fixes)
- kit: 0.5.0
- passes: reviewer ran · business degraded (no filled BUSINESS.md — "nothing declared") · decision ran · compliance ran
- verdict: ⚠️ commit after changes — all addressed below, then re-verified
- findings: 0 blocker · 2 major · 8 minor · 2 nit
  - major · reviewer + /decision · the `.gitattributes` narrowing rewrote the file wholesale, so
    it discarded `.attest/*.md text eol=lf` — the pin **this same series** added — handing the
    template path back the CRLF record misdiagnosis the installer path had just closed; and on a
    late run it would have silently dropped any line the adopter had added under attest's header.
    Now surgical: only the blanket `*.sh` line goes. Symlinked or unwritable files are reported,
    never written through.
  - major · reviewer + /decision · ADR-0044's Consequences claimed *"smoke asserts in both
    directions"* for the new CRLF branch; `grep 'LINE ENDINGS' scripts/smoke.sh` returned nothing.
    The consequence had been written from a manual check. Three assertions added, and the entry
    now states what they assert. Caught **before** the entry landed, so it was corrected rather
    than narrowed — append-only binds a written entry, not a pending one.
  - minor ×8 · /business: the fix covered the workflow but not the manual path README itself
    describes (delete `scripts/`, Actions disabled, never run) — README gains a step C, marked
    *do this even if you skip A and B* · README and GUIDE still promised "one line" to
    `.gitattributes` while `install.sh` now writes two · /compliance: the ladder's closing
    sentence covered both sanctioned mutations but is true only of the sweep — the redaction is
    precisely a project editing its own record · ADR-0042 did not name that a retained record
    carries the maintainer's identity · reviewer: outside a repo `--is-shallow-repository` prints
    nothing, so the new arm swallowed the no-git case and made its `else` dead code — the three
    questions are now asked in the order that narrows (repo · has a commit · complete history),
    which also closed an unnoticed hole: a repo before its first commit answers `false` and would
    have had every record swept · the orphaned `.attest` comment · *"is no longer attest's"* for a
    file that never existed · the installer's report named one pin while writing two
  - nit ×2 · two assertions claiming the same fact about one line (one removed) · `> file` vs
    `install.sh`'s explicit symlink refusal (resolved by the surgical rewrite)
- note: **the first round of fixes introduced two of these.** That is the argument for running the
  gate a second time over its own repairs rather than trusting them, and it is why the 10:30Z
  verdict was not simply marked closed.
- note: a `fail`-not-`ok` correction in the new clone fixture — an unbuildable fixture scored a
  pass, which is the mode the suite's own comment records as having once cost it 75 assertions.
- verification: `smoke.sh` 156 → **166**, shellcheck clean. The six new assertions were run in a
  worktree at the pre-fix HEAD and **fail there**, including `a shallow clone keeps every record
  (mine=GONE)`; a test that cannot fail is not a regression test. Install shape re-measured:
  16 files + 2 `.gitignore` + 2 `.gitattributes` = 20 items, 22 with `--compliance`.
