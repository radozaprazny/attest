# ship audit — 2026-09-07 (/audit-history, default mode)

- HEAD: 0a24f6f (feat/heading-overrides) · tree: clean · nothing untracked
- kit: 0.6.0
- findings: 0 blocker · 0 major · 0 minor
- mode: default — scope `main..HEAD` (7 files, +184/-18: the heading-override series, the
  0.6.0 cut, and the PR #10 ship record committed at last).
- verdict: ✅ clean — nothing found that must not ship

## What was scanned

The three commits about to be pushed, plus the working tree (clean, `git status --porcelain`
empty). No scanner is configured in this repo — `gitleaks`/`trufflehog` are not installed — so
the taxonomy was run as greps over `git diff main..HEAD`, plus a history-wide blob check for
key material across `git rev-list --all`.

- **blocker classes** — secrets, private keys, credential assignments, connection strings,
  cloud keys: empty on the added lines and empty across the whole history. Special-category
  data (GDPR Art 9) and national identifiers (rodné číslo, IČO, DIČ shapes): empty.
- **major classes** — emails, phone numbers, IP addresses, third-party personal names, client
  or customer names: empty on the added lines. Two matches were weighed and dismissed, both
  inside `.attest/ship-20260906-115723-d91f69f.md` quoted by the record this series committed:
  `adopter@example.invalid` and `alice@example.com` are RFC 2606 reserved domains used as
  fixtures and as the worked example in the compliance skill — not data about a person.
- **minor classes** — absolute paths, usernames, internal hostnames, stray large or binary
  files: empty. Largest file touched is `docs/attest-decisions.md` at 104 kB of prose; no
  binary blobs in the range.

## Project boundaries

`BUSINESS.md` and `COMPLIANCE.md` in this repo are still the shipped templates — their
non-goals and data-handling sections hold placeholder text — so there is no project-declared
data class to check beyond the generic taxonomy. The one local store the kit itself creates,
`.attest/tmp/ship-guard.log`, is confirmed gitignored (`.gitignore:6`) and has no tracked
files under it; it does not leave this machine.

## Remediation

None required.
