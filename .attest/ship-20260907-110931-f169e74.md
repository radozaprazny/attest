# ship audit — 2026-09-07 (/audit-history, default mode)

- HEAD: f169e74 (fix/guard-trace-and-semantics) · tree: clean · nothing untracked
- kit: 0.5.0
- findings: 0 blocker · 0 major · 0 minor
- mode: default — scope `main..HEAD` (25 files, +1418/-73: the whole of PR #10, i.e. phase 12
  plus everything two `/gate` runs added to it).
- verdict: ✅ clean — nothing found that must not ship
- degraded: one pass, as on every record here — `BUSINESS.md` Non-goals and `COMPLIANCE.md` are
  unfilled kit templates by design (ADR-0006), so step 2 had no project boundaries to layer on
  the generic taxonomy. Everything else ran in full.
- scanned, empty on the added lines: secrets, private keys and credential assignments ·
  special-category data · national identifiers · client names · IP addresses · internal
  hostnames · stray large or data files.
- the three address- and path-shaped hits are illustrative and were already recorded in
  `ship-20260906-115723-d91f69f.md`: `adopter@example.invalid` (smoke fixture, RFC 2606 reserved
  TLD), and `alice@example.com` with `/home/alice/patients-2026.csv` — the worked example in
  GUIDE 2.2 and ADR-0038 showing what a guard trace line *can* hold, which is the argument for
  keeping the trace in ignored scratch. `example.com` is RFC 2606 reserved; no real subject.
- remediation: none required.
- note: written in its own step, before the push it gates (ADR-0033), so it is untracked as the
  guard reads it and will land under a later sha than the one it names.
