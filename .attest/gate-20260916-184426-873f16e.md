# gate run — 2026-09-16 (round 1; recorded retrospectively)

> **Honesty note.** Written at 2026-09-16T18:44:26Z, after the fact. The passes ran earlier the
> same day and their own clock times were not kept, so the filename carries the WRITING time
> (attest ADR-0032). The round-2 record beside it was written in the same sitting.

- HEAD: 873f16e (feat/guard-betterleaks) · tree: dirty — gated the working diff
- kit: 0.10.0
- mode: light — the passes stage 0 said the diff touched
- round: 1/3 (fix)
- triggers: reviewer run (8 paths outside .attest/) · decision run (1 hit) · compliance run (1 hit) · posture none · business skip (nothing declared)
- passes: reviewer ran · business skipped (nothing declared) · decision ran · compliance skipped (template)
- verdict: ⚠️ commit after changes — closed in round 2, recorded beside this
- findings: 0 blocker · 3 major · 9 minor · 2 nit
  - major · reviewer · a suggested command that prints the value the prompt withholds · .claude/hooks/ship_guard.sh
  - major · decision · a threshold stated as measured with no measurement behind it · docs/attest-decisions.md
  - major · decision · a trace word that hides a distinction an earlier entry kept · .claude/hooks/ship_guard.sh
  - minor ×9 · reviewer, decision
  - nit ×2 · reviewer
- notes:
  - `/compliance` skipped although stage 0 said run: its one trigger was a test-fixture address in
    `scripts/smoke.sh`, and `COMPLIANCE.md` here is the shipped template, as in every earlier
    record. The one regulated-ground question this change raises — does anything leave the
    machine — was answered by measurement instead: a traced scan made no socket call.
  - **The repair round found two silent misses no pass had reported**, both by measuring what a
    finding only questioned. The scanner's own time limit reads a partial scan as clean at random,
    so the hook now stops the scanner itself. And the guard as released in v0.9.0 honoured an
    inherited `KIND` variable, which let a push with no record through unasked; the hook now
    empties its own state first.
- detail: the session output; a durable copy under `.attest/tmp/` (ignored) if you want one
