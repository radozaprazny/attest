# ship audit — 2026-09-01 (/audit-history, default mode)

- HEAD: 2455139 (docs/adopter-onboarding) · tree: clean · nothing untracked
- kit: 0.3.0
- mode: default — scope `main..HEAD` (2 files: README's onboarding split and sample line, the
  thread-carrier's measured case for going public and one piece of recorded debt).
- verdict: ✅ clean — after one finding of its own, fixed before the push
- findings: 0 blocker · 0 major · 0 minor · 0 nit **at the audited sha**
- **found and fixed during this run (major, ordinary personal data):** the first draft of the
  carrier spelled the maintainer's address out in prose, to explain that it sits in 45 commit
  authorships. That moves it from **authorship metadata into tracked file content** — a
  different exposure class under the taxonomy, and the one the taxonomy calls the real one — for
  no gain, since the sentence carries its point without the literal string. Replaced with "the
  maintainer's address" and the commit amended before anything was pushed. Recorded here rather
  than silently corrected: the gate caught a real change in exposure class on its author's own
  work, which is the case it exists for.
- degraded: the usual single pass — `BUSINESS.md` Non-goals and `COMPLIANCE.md` are unfilled kit
  templates by design (ADR-0006), so step 2 had no project boundaries to layer on the generic
  taxonomy. Everything else ran in full.
- scanned, all empty on the added lines at 2455139: secrets and private keys · special-category
  data · national identifiers · **ordinary personal data (no address is added by this delta)** ·
  client names · IP addresses · internal hostnames and absolute home paths · stray large or data
  files. The delta is prose in one shipped document and one of attest's own.
- remediation: none outstanding.
- note: written in its own step, before the push it gates (ADR-0033).
