# gate run — 2026-09-13 (round 2; recorded retrospectively)

> **Honesty note.** Written at 2026-09-13T20:43:42Z, after the fact, for the repair cycle that
> followed the round recorded beside this one. The run's own clock time was not kept, so the
> filename carries the writing time (attest ADR-0032).

- HEAD: 448b57b (fix/guard-normalise-matching) · tree: dirty — gated the fix hunks
- kit: 0.9.0
- mode: light — the repair cycle for round 1
- round: 2/3 (fix)
- triggers: not re-run — the fix hunks stayed inside the files round 1 already named, so no document pass was re-opened
- passes: reviewer re-review (fix hunks) · business skipped (nothing declared) · decision skipped (no new trigger on the fix hunks) · compliance skipped (no trigger)
- verdict: ⚠️ commit after changes — the major closed in round 3, recorded beside this
- findings: 0 blocker · 1 major · 4 minor · 1 nit
  - major · reviewer · an option on the separate-argument list that takes no separate argument · .claude/hooks/ship_guard.sh
  - minor ×4 · reviewer
  - nit ×1 · reviewer
- notes:
  - **Round 1's eight REVIEWER-OWNED findings came back seven addressed, one not addressed, none
    regressed.** Eight, not the fifteen the round-1 record totals: a re-review is handed only its
    own previous findings (ADR-0068), and the other seven belong to the `/decision` pass, which
    did not re-run.
    The one not addressed was the duplicated counts, which had gone from three homes to two
    rather than to one; it is closed in round 3.
  - **The major is the round earning its cost, and it is a measurement error rather than a code
    one.** Round 1's repair added `--exec-path` to the list of git global options that take a
    separate argument, on the strength of a probe that asked whether the command **exited 0**.
    It does — it prints the exec path and stops, never reaching the subcommand. Asking instead
    whether the **subcommand ran** removes `--exec-path` and nothing else. No push escaped
    through it, because that spelling of the command pushes nothing; what it broke was the
    claim, stated as measured in three files, that the list is exact.
  - A proxy for a question is not the question. This is the failure the repository's own rule
    already names, found by the pass whose job is to re-read what the last one wrote.
- detail: the session output; a durable copy under `.attest/tmp/` (ignored) if you want one
