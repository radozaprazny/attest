# gate run — 2026-09-13 (round 1; recorded retrospectively)

> **Honesty note.** Written at 2026-09-13T20:43:25Z, after the fact. The passes ran earlier in
> the same session and their own clock times were not kept, so the filename carries the WRITING
> time (attest ADR-0032). It was written in one sitting with the round-2 record beside it, which
> is why the two share a timestamp band and neither claims otherwise.

- HEAD: 448b57b (fix/guard-normalise-matching) · tree: dirty — gated the working diff
- kit: 0.9.0
- mode: light — the passes stage 0 said the diff touched
- round: 1/3 (fix)
- triggers: reviewer run (6 paths outside .attest/) · decision run (1 hit) · compliance skip (no trigger) · business skip (nothing declared)
- passes: reviewer ran · business skipped (nothing declared) · decision ran · compliance skipped (no trigger)
- verdict: ⚠️ commit after changes — closed across rounds 2 and 3, recorded beside this
- findings: 0 blocker · 6 major · 6 minor · 3 nit
  - major · reviewer · a separate-argument option list too short to reach the subcommand · .claude/hooks/ship_guard.sh
  - major · reviewer · an escape hatch a flag's value still fits through · .claude/hooks/ship_guard.sh
  - major · decision · a relation pointing at an entry that owns no such rule · docs/attest-decisions.md
  - major · decision · two deferred findings presented as new discoveries · docs/attest-decisions.md
  - major · decision · a stated limit that is the opposite of what the code does · docs/attest-decisions.md
  - major · decision · a relation spelling the log's header forbids repeating · docs/attest-decisions.md
  - minor ×6 · reviewer, decision
  - nit ×3 · reviewer
- notes:
  - Run in `light` mode: stage 0 chose reviewer and decision, and skipped business and
    compliance. The `posture none` line was raised and carried, per ADR-0067 — `COMPLIANCE.md`
    here is the shipped template and attest keeps its posture elsewhere.
  - **Both passes found a class the other could not.** The reviewer owns the hook and found the
    silent miss; the `/decision` pass owns the log and found the entry claiming, as new, two
    holes ADR-0038 had written down on 2026-09-04 and deferred on purpose. Neither finding is
    reachable from the other's ground.
  - Every finding was reproduced against `448b57b` before being acted on, and one was reproduced
    and found to be **pre-existing rather than a regression** (`cp 'x;y' .attest/ship-a.md`),
    which is why it is recorded as a known limit instead of repaired here.
- detail: the session output; a durable copy under `.attest/tmp/` (ignored) if you want one
