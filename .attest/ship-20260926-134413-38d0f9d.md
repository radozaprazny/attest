# ship audit — 2026-09-26 (/audit-history, default mode)

- HEAD: 38d0f9d (fix/guard-scan-unpushed-refs) · tree: clean · nothing untracked
- findings: 0 blocker · 0 major · 0 minor
- kit: 0.12.1
- mode: default — scope `HEAD --not --remotes` (one commit, 38d0f9d, base b96c39a on
  origin/main), the working tree, the index and untracked files (none). No push target exists
  yet, so `@{push}` was not available. Run by the session that wrote the change, not by an
  independent pass; the diff had one read-only `reviewer` pass before it was committed.
- verdict: ✅ clean — nothing found that must not ship
- key layer: betterleaks 1.8.1 — unpushed commits exit 0, and exit 0 over the guard's wider range
  this change introduces (`HEAD --branches --tags --not --remotes`, ADR-0074) · unstaged and
  staged: exit 0 · untracked: none, so the `dir` call was skipped as step 4 says.
- scanned, empty: private keys and token-shaped values · special-category data · national
  identifiers · personal names, emails, phone numbers and IP addresses in the 144 added lines ·
  client and third-party names, the adopter included · internal hostnames and absolute machine
  paths, Windows-style ones included · URLs · large or binary files. Pattern hits triaged as not
  data: one address on the reserved `.invalid` test domain the suite already uses, and one
  fixture record id shaped like a phone number.
- declarations read: `BUSINESS.md` and `COMPLIANCE.md` are the shipped templates; attest's own
  `docs/attest-progress.md` and decision log change on this branch, with no new data class.
- authorship: the commit carries the maintainer's own name and address, and a Co-Authored-By
  line for the model — intended, in their own public repository.
- degraded: none.
- remediation: none required.
- note: written before the push it gates (ADR-0033), so it lands under a later sha than it names.
