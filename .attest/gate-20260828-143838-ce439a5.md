# gate run — 2026-08-28T14:38:38Z

- HEAD: ce439a5 (feat/lean-kit) · tree: dirty — gated the working diff (phase 11, the lean-kit series)
- kit: 0.3.0
- passes: reviewer ran · business degraded (no filled BUSINESS.md — "nothing declared") · decision ran · compliance ran
- verdict: ⚠️ commit after changes
- findings: 0 blocker · 3 major · 9 minor · 1 nit
  - major · reviewer · index state — only the six deletions were staged, so a bare `git commit`
    would land a tree whose settings.json still registers three deleted hooks and whose smoke test
    still tests them. Not a defect in the change: `git add -A` before committing
  - major · /decision + /compliance · `business/SKILL.md` fixed the inherited regulated-ground
    finding at **major**, overriding the ladder's always-a-blocker floor — so removing /compliance
    silently downgraded a blocker, the opposite of what ADR-0030 promises. Severity now comes from
    the ladder; the carve-out's ground list was also narrower than the row it inherits
  - major · /compliance · the "unfilled COMPLIANCE.md" check sat behind the per-diff trigger, so a
    template-generated repo with the skill installed and an empty posture file could run
    indefinitely with no pass ever saying so — the second gate's hole had moved, not closed
  - minor ×9 · a blockquote in Mode 3 swallowed the content-vs-behaviour rule via lazy continuation
    (three passes found it independently); `/decision` and GUIDE still called the re-assigned ground
    "ownerless"; the ladder's /business ownership row lacked the carve-out annotation; README and
    GUIDE said the ship guard "stops" a push when it asks; stale counts in attest-progress (cap) and
    attest-devlog (assertions); "four invocations"/"four audits inline" left over from the pre-opt-in
    gate; a stale "the gate has not run" plan item
  - nit ×1 · an orphaned line in README after an earlier deletion
- note: third full gate on this tree. All findings above were addressed before commit; baseline
  re-run green afterwards — shellcheck clean, smoke 99 passed / 0 failed. Prior runs on the same
  HEAD: gate-20260828-125725 (4 major) and the second pass whose two majors this run's fixes
  followed on from. This record attests the run that found them, not the state after the fixes.
