# gate run — 2026-08-28T14:52:55Z

- HEAD: ce439a5 (feat/lean-kit) · tree: dirty — gated the working diff (phase 11, the lean-kit series)
- kit: 0.3.0
- passes: reviewer ran · business degraded (no filled BUSINESS.md — "nothing declared") · decision ran · compliance ran
- verdict: ⚠️ commit after changes
- findings: 0 blocker · 5 major · 12 minor · 2 nit
  - major · reviewer · index state — still only six deletions staged; `git add -A` before commit
  - major · reviewer · `compliance/SKILL.md` — "the trigger check: do this first, and **only**
    this. Read the diff alone: no COMPLIANCE.md" sat immediately above the new posture check that
    reads COMPLIANCE.md. An agent following it literally never ran the check the previous gate
    added. Restructured as an explicit Step 0 ahead of the trigger
  - major · reviewer · `decision/SKILL.md` — its fallback paragraph carried no severity rule
    while its own rung list fixes "major", so an inherited Art 9 / national-ID hunk was
    downgraded below the ladder floor — the same defect fixed on /business one gate earlier, and
    ADR-0030 already claimed the two rows were symmetric
  - major · /compliance · a third silent state, created by the previous gate's own remedy text:
    it told the user to "delete it" (COMPLIANCE.md) while leaving the skill installed. Then the
    ladder does not re-assign (directory present), /business's carve-out is off, /decision defers,
    and the posture check was keyed on the file existing. Now keyed on "no posture declared"
    (absent OR placeholder) and the remedy names both paths
  - major · /decision · the retrospectively written record introduced a rule with no home and
    broke the assumption that name order is run order → ADR-0032
  - minor ×12 · a duplicated verdict-line rule in /business whose two copies had already diverged;
    the posture fact prescribed twice in /compliance; /compliance's own rung list sitting below
    the ladder floor it hands on; README and GUIDE still claiming only CLAUDE.md is ever in
    context (the SessionStart hook falsifies it); GUIDE PART 2's intro and /business's frontmatter
    description left behind by two earlier fixes; a stale assertion count; ci.yml overstating
    ADR-0027; three smoke assertions checking one of the two things they name; no escape in the
    posture check for a repo that ships the documents as a product
  - nit ×2 · the JSON assertion failed rather than skipped without python3 — in a kit that had
    just dropped that dependency; a re-wrap leftover in README
- note: fourth full gate on this tree. The reviewer's verdict on the CODE was clean for the second
  run running — every finding above except the index state is in instruction prose. All were
  addressed; baseline re-run green afterwards (shellcheck clean, smoke 104 passed / 0 failed, up
  from 99 — the new assertions split three that had been checking one file of two, and the JSON
  one now skips rather than fails without python3). Records on this tree: 125725, 143838, 144100
  (late, self-declared) and this one.
