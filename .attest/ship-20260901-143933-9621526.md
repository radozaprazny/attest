# ship audit — 2026-09-01 (/audit-history, **full** mode)

- HEAD: 9621526 (main) · tree: clean apart from one untracked record, committed with this one
- kit: 0.3.0
- mode: **full** — the entire history, not the current tree: **51 commits** across **9 refs**
  (`main`, four topic branches and their remotes), **441 objects**, and every one of the
  **44 paths that ever existed**, including the ten deleted downstream (`format_py.py`,
  `ruff.toml`, both retired `.py` hooks, `init-tier/SKILL.md`, `.mcp.json.example`,
  `ci.yml.example`). Patterns were fed the commit list — `git grep <pat> $(git rev-list --all)` —
  so old blobs were reached, which a bare `git grep` cannot do. Run because `full` is the mode
  the skill requires before a public release, and going public is the open decision.
- verdict: ✅ clean — nothing in 51 commits that must not ship
- findings: 0 blocker · 0 major · 0 minor · 0 nit

## What was scanned, and what came back

- **secrets / private keys / tokens** — none. No `BEGIN … PRIVATE KEY`, no `AKIA…`, `ghp_`,
  `gho_`, `xox[baprs]-`, `sk-…`, `AIza…` in any blob at any revision; no
  `password|secret|api_key|token = "…"` assignment anywhere; no `.env`, `.pem`, `.key`,
  `id_rsa`, `.p12`, `.pfx` or credential file was **ever added** (checked by
  `--diff-filter=A` over all refs, not just the current tree).
- **special-category data (GDPR Art 9) · national identifiers** — none. No birth-number,
  passport or tax-number shapes; no health, biometric or other Art 9 class.
- **ordinary personal data** — exactly **one** address in 51 commits of file content:
  `smoke@example.invalid`, 33 occurrences, the git identity of `smoke.sh`'s fixture repos.
  Reserved TLD, undeliverable by RFC. Commit messages hold only `noreply@anthropic.com`
  (45×, the `Co-Authored-By` trailer).
- **client / customer names · third parties** — none. Two commit identities exist in the whole
  history and both are the maintainer's; no other person is named in content or in a trailer.
- **IP addresses · geolocation · device identifiers** — none.
- **internal hostnames / absolute paths / usernames** — none. No `/home/<user>`,
  `/Users/<user>` or `C:\Users` at any revision.
- **stray large or data files** — none. The largest blob ever committed is
  `docs/attest-decisions.md` at 74 KB; every one of the 44 paths is markdown, shell, YAML,
  TOML or (historically) Python.
- **tags, git notes, stash, unreachable objects** — 0, 0, 0, and `git count-objects -v`
  reports no garbage, so nothing ships outside the reachable graph.
- **the project's own declarations** — `BUSINESS.md` still carries placeholder markup at all
  51 revisions; it was never filled with real content and then emptied.

## Intended, and worth stating before the visibility decision

Neither is a finding. Both are the maintainer's own identity in a repo he owns and intends to
publish — the taxonomy's context rule. They are recorded because a public flip is irreversible
and the decision should be made knowing them.

- **`rado@zaprazny.sk` appears in the authorship of 45 commits** (the other 6 are the GitHub
  noreply used by web merges). Publishing makes that address world-readable and harvestable,
  permanently. Setting a GitHub noreply address now would change future commits only — it would
  not rewrite the 45 already written, and rewriting them is not worth it for a maintainer's own
  address.
- **The name appears in file content in three necessary places**: the `LICENSE` copyright line,
  the `github.com/radozaprazny/attest` clone URLs and attribution in `README.md` and
  `scripts/template-cleanup.sh`, and the hard-coded repo-name guards in `ci.yml` and
  `template-cleanup.yml` — the latter load-bearing per ADR-0012 and never to be softened.

## Degraded passes

One, the same as every record on this repo: step 2 (read the project's own boundaries) had
nothing to read. `BUSINESS.md` Non-goals and `COMPLIANCE.md` are unfilled kit templates by
design (ADR-0006), so no project-specific leak class could be layered on the generic taxonomy.
attest's own posture is declared in `docs/attest-progress.md` and names no data class at all.
Everything else ran in full.

## Remediation

None required. Nothing to rotate, nothing to purge, no force-push. No personal-data breach
question arises (GDPR Art 33/34): nothing about a third party was ever committed.

## Notes

- Written on `chore/full-audit-record`, before the push it gates (ADR-0033).
- Committed alongside `ship-20260901-110352-604bf0d.md`, the previous record, which per
  ADR-0033 could not be contained by the commit it names and had stayed untracked through the
  PR #6 cycle. Both land here; each is evidence about the sha **in its own name**.
