# ship audit — 2026-09-13 (/audit-history, default mode)

- HEAD: 8337b5e (feat/gate-fix) · tree: clean · nothing untracked
- kit: 0.9.0
- findings: 0 blocker · 0 major · 0 minor
- mode: default — scope `origin/main...HEAD`: one commit, 11 files, 396 added lines, the commit
  message, the working tree and the index, plus the 415-line file this change deletes, read from
  `origin/main` to check that nothing travelled out of it. Run by an independent pass, not by the
  session that wrote the change (ADR-0051).
- verdict: ✅ clean — nothing found that must not ship
- scanned, empty on the added lines: secrets and credential assignments · special-category data ·
  national identifiers · personal names and addresses · client names · IP addresses · email
  addresses · internal hostnames and absolute machine paths · session identifiers · stray large,
  binary or data files.
- the deletion: the removed proposal carried nothing from the taxonomy, and exactly one of its
  lines survives into the diff — its own `Decision` sentence, quoted into ADR-0068.
- **what the pass found that was not a leak, and why it is written here anyway:** two false
  statements in the run record this push would have published — a count that matched no list,
  and a claim that the loop stopped at its finish line when the same record reports two majors.
  Both were corrected before the push, visibly, in the record itself. A ship gate is not supposed
  to be the thing that catches an inaccurate attestation; that it did is worth more in this
  record than a clean line would have been.
- degraded: one part — no generic scanner (`gitleaks`, `trufflehog`) is installed or in CI, so
  the key-regex layer was the pass's own set rather than a maintained rule-pack.
- remediation: none required.
- note: written before the push it gates (ADR-0033), so it lands under a later sha than it names.
