# gate run — 2026-09-06T11:37:57Z

- HEAD: 93fc862 (fix/guard-trace-and-semantics) · tree: dirty — gated the working diff (phase 12, second pass)
- kit: 0.5.0
- passes: reviewer ran · business degraded (no filled BUSINESS.md — "nothing declared") · decision ran · compliance ran
- verdict: ⚠️ commit after changes
- findings: 0 blocker · 4 major · 13 minor · 4 nit
  - major · reviewer · `template-cleanup.sh` took the last dash-segment of a filename as a
    commit-ish, so an adopter's record with a name the kit never writes — `gate-notes.md`,
    `ship-…-<sha>-rerun.md`, `gate-2026-09-05-pre-release.md` — was fed to `git cat-file`, failed,
    and was deleted. Measured: 3 of 4 gone. A narrower cousin of the blocker the previous gate
    caught, in the same unattended script. Now: a tail that is not bare hex of length ≥ 4 is kept
    unexamined
  - major · reviewer · the guard passed on **any** clean record for the sha, and the glob expands
    lexicographically, so the oldest won. Quick scan clean → `full` scan finds a blocker → push
    goes through silently, which is the workflow the guard's own prompt recommends. Now: every
    record for the sha must be clean
  - major · reviewer · one unguarded `ga_lines=$(grep -c …)` under `set -e` killed 75 of 149
    assertions with no verdict — including the regression test for the previous gate's blocker
  - major · /business · `install.sh` wrote `*.sh text eol=lf` into the adopter's repo. `text`
    normalises on `git add`, so it would rewrite their own CRLF scripts under a rule the
    installer put there — against "nothing in this kit edits your code". The `.claude/hooks/*`
    line already covers 100% of the kit's footprint, so the blanket pattern is withdrawn
  - minor ×13 · the parser's two greps could be satisfied by prose at column 0 (now reads the
    first header line of each kind) · a failed CR strip silently truncated a hook and still
    reported ✓ (now restores from the kit and says so) · a smoke assertion claimed "readable"
    and tested "and clean" · the ladder still named one `.attest/` exception · the `Narrows:`
    relation had four instances and no mechanism · ADR-0039 did not record its third mechanism ·
    ADR-0038's four trace words never reached GUIDE · three summary lines still promised
    unqualified coverage · the trace keep-set omitted space and `/`, so file paths · the
    `COMPLIANCE.md` notice lacked per-checkout scope and the no-rotation fact, and `/compliance`
    Mode 1 would have written over it · the ✅ block in the carrier contradicted what shipped ·
    devlog assertion base was wrong
  - nit ×4 · a dead `cp` in the CRLF fixture, an inexact `says_not '^2'`, scope mismatch between
    the strip and the attribute, two overlong lines
- note: the reviewer verified the previous round's fixes by **mutation** — reverting each of five
  and confirming the suite catches it (5/5). ADR-0040 was split into 0040 (redaction) and 0041
  (the sweep) before commit, because `Supersedes:` and the `Status` flip both operate on a whole
  entry and the window closes at commit; the earlier gate record still cites the sweep as 0040,
  which is what it was called then, and is not edited (ADR-0032). Every finding addressed;
  baseline re-run green — shellcheck clean, smoke 149 → **156** passed / 0 failed.
