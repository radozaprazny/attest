# ship audit — 2026-09-13 (/audit-history, default mode)

- HEAD: fdd3c7a (fix/guard-normalise-matching) · tree: clean · nothing untracked but ignored scratch
- kit: 0.9.0
- mode: default — scope `origin/main...HEAD`: one commit, 9 files, +490/-32, the commit message,
  the working tree and the index. Run by an independent pass, not by the session that wrote the
  change (ADR-0051).
- verdict: ✅ clean — nothing found that must not ship
- findings: 0 blocker · 0 major · 0 minor
- scanned, empty on the added lines: secrets and key shapes (`BEGIN … PRIVATE KEY`, `AKIA`,
  `ghp_`, `sk-`, `Bearer`, `api_key`, `password`) · `.env` and connection strings ·
  special-category data · national identifiers · personal names and addresses · email addresses
  (0) · phone numbers · IP addresses (0) · added URLs (0) · client and third-party names ·
  internal hostnames and absolute machine paths · session identifiers · stray large, binary or
  data files (largest added blob 2671 B).
- **the sha this record names is not the sha the scan ran over, and that is stated rather than
  smoothed over.** The pass ran against `03df398` and returned ✅ with nothing to remediate. It
  also reported two prose counts the tree did not back — an entry saying *five* smoke cases pin
  a list of seven, and a run record saying *eight findings* where it meant eight owned by one
  pass. Both were corrected and the commit amended to `fdd3c7a` before any push, which is the
  only window in which that is not an edit to history (ADR-0057). The amend touched
  `docs/attest-decisions.md` and one `.attest/gate-*` record, prose only: no code, no new file,
  no added material for the taxonomy to scan. The ✅ therefore carries to this sha, and a reader
  who wants to check that claim has the two shas to diff.
- on the three `.attest/gate-*` records this commit adds: read against ADR-0049 and at
  attestation altitude — severity, pass, class, path per finding, no values and no line numbers.
  Their narrative sections are covered by attest's own declared widening of that rule for its own
  records. Nothing in them names a person, a client, a machine, or a path outside the repository.
- weighed and deliberately not flagged: `John Doe` in a hook comment and in ADR-0069 is the
  anonymisation placeholder the sentence is *about* — a quoted value holding a space — and
  identifies nobody. The `/tmp` strings in the diff are `smoke.sh` fixtures and the repo-relative
  `.attest/tmp/`; none names a session, a user or a host.
- degraded: one part — no generic scanner (`gitleaks`, `trufflehog`) is installed or in CI, so
  the key-regex layer was the pass's own set rather than a maintained rule-pack.
- remediation: none required.
- note: written before the push it gates (ADR-0033). Covers a push made through a shell; a
  publish made through an MCP server asks regardless and no record answers for it (ADR-0058).
