# gate run — 2026-09-12T19:41:20Z

> **Scope deviation, declared.** The tree was clean, so the skill's default diff would have been
> `git show HEAD` — one commit. This run gated `1028991..HEAD` instead: the six commits of this
> branch's own work, above the merge of PR #18 and PR #19. Those two were gated in their own
> sessions (#19 left `gate-20260912-095730-92a6b52.md`; #18 left none).
>
> **The gated commits were rewritten after this run and before any push, and this record says
> so rather than pretending otherwise** (the honesty rule of ADR-0032, applied to a sha instead
> of a time). The run gated `5bbad84`, which no longer exists: the `/audit-history` pass that
> followed found that a third party's project name and three of its specific weaknesses, removed
> from the tree, still sat in the commits about to be pushed. Removing them meant rebuilding two
> unpushed commits — `5bbad84` is now `4979896`. What changed between them is devlog prose that
> **no finding in this record referenced**; every finding, count and verdict below is untouched.

- HEAD: 4979896 (integration) · tree: clean — gated `1028991..HEAD`, not the last commit alone
- kit: 0.8.0
- passes: reviewer ran · business skipped (template) · decision ran · compliance skipped (template — wrongly; see below)
- verdict: ⚠️ commit after changes — every major addressed in the commit this record travels with, one rejected with evidence
- findings: 0 blocker · 7 major · 7 minor · 5 nit
  - major · reviewer · id-allocation command matches a commented template entry · .claude/skills/decision/SKILL.md
  - major · reviewer · grammar check weaker than the claim made for it · scripts/smoke.sh
  - major · reviewer · miscounted run records in shipped instruction text · .claude/skills/gate/SKILL.md
  - major · reviewer · two time spans conflated in a measured claim · .claude/skills/_shared/audit-ladder.md
  - major · decision · a skip rule scoped wider than the entry sanctioning it · .claude/skills/gate/SKILL.md
  - major · decision · relation field misdescribes the entry it points at · docs/attest-decisions.md
  - major · decision · two mutually exclusive statements of one invariant · DECISIONS.md
  - minor ×7 · reviewer, decision — carried to `docs/attest-progress.md` *Next*, unfixed (ADR-0061)
  - nit ×5 · reviewer
- notes:
  - **An eighth major was raised and rejected.** The reviewer read one series' rounds in
    file-name order; the record in question declares itself written late, so name order is not
    run order (ADR-0032). The `/decision` pass had verified the same sequence independently and
    found it correct. The claim stands as written.
  - **`/compliance` was skipped and should not have been.** This run applied the skeleton rule
    to every document pass; the run itself found that wrong, and ADR-0066 now scopes it to
    `/business`, whose ground is its document. The `/decision` pass reported the one regulated
    ground item under the ladder's fallback, naming what it would have been.
  - Two of the seven minors were factual claims this branch had introduced and were corrected in
    the same breath, rather than carried: a mislabelled count and a sentence that contradicted
    the diff it described. The other five are in *Next*, and no further round was run.
- detail: the session output; a durable copy under `.attest/tmp/` (ignored) if you want one
