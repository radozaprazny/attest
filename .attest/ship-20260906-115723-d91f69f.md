# ship audit — 2026-09-06 (/audit-history, default mode)

- HEAD: d91f69f (fix/guard-trace-and-semantics) · tree: clean · one untracked file — this record
- kit: 0.5.0
- mode: default — scope `main..HEAD`, 19 files, +867/−67 (the phase-12 series, ADR-0037…0041)
- verdict: ✅ clean — nothing found that must not ship
- findings: 0 blocker · 0 major · 0 minor · 0 nit
- scanned, all empty on the added lines: secrets and private keys · special-category data ·
  national identifiers · IP addresses · client or customer names · internal hostnames · stray
  large or data files.
- two addresses appear and both are fictional by construction: `adopter@example.invalid` (a
  smoke fixture; `.invalid` is reserved by RFC 2606) and `alice@example.com` (a GUIDE example
  of what the guard's trace can hold; `example.com` is reserved by the same RFC). One absolute
  path, `/home/alice/patients-2026.csv`, is the illustrative argument in that same GUIDE
  paragraph — no such file, and naming a plausible one is the point of the sentence.
- the maintainer's address: **0 occurrences** anywhere in the working tree. It was redacted from
  `ship-20260901-143933-9621526.md` under ADR-0040 in this very series; the pre-redaction blob at
  `93fc862` is unchanged and that is recorded there as an accepted risk, not closed by this scan.
- degraded: the usual single pass — `BUSINESS.md` and `COMPLIANCE.md` are unfilled kit templates
  by design (ADR-0006), so step 2 had no project boundaries to layer on the generic taxonomy.
- remediation: none required.
- note: written before the push it gates (ADR-0033), so it is untracked at that moment and will
  be committed under a later sha.
