# ship audit — 2026-09-16 (/audit-history, default mode)

- HEAD: 3a9a1a6 (feat/guard-betterleaks) · tree: clean · nothing untracked
- findings: 0 blocker · 0 major · 0 minor
- kit: 0.10.0
- mode: default — scope `origin/main...HEAD` (71304f1, 3a9a1a6), the working tree, the index and
  untracked files (none). Run by an independent pass, not by the session that wrote the change
  (ADR-0051).
- verdict: ✅ clean — nothing found that must not ship
- key layer: betterleaks 1.8.1, the first ship record written with it — unpushed commits exit 0 ·
  unstaged exit 0 · staged exit 0 · untracked: none, so the `dir` call was skipped as step 4 says.
- scanned, empty: special-category data · national identifiers · personal names, emails and IP
  addresses (one `example.invalid` test address) · client and third-party names · internal
  hostnames and absolute machine paths · large or binary files.
- **three independent passes, not one, and the first two did not clear this branch.** The pass
  at `b6b7794` found a status note contradicted by the records it described, and the suite's own
  stub secret tripping a key rule; the pass at `11a1cb4` found the repaired note outdated by a
  newer record and a skill sentence the tool's behaviour contradicted. Each time the unpushed
  commits were rebuilt, never amended past a push (ADR-0057), and the next pass read the whole
  difference. This record is about `3a9a1a6` only.
- degraded: none.
- remediation: none required.
- note: written before the push it gates (ADR-0033), so it lands under a later sha than it names.
